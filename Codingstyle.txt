funktionstyp funktionInCamelCase(parameter, parameter)
{
	Funktionsinhalt
}


if (Bedingung)
{
	...
} 
else if (Bedingung) 
{
	...
}
else
{
	...
}


for (...)
{
	...
}

IMMER KORREKT EINRÜCKEN! Wenn irgendwo eine geschweifte Klammer aufgemacht wird, ist danach alles einen Tab eingerückt. Sonst wird das
alles extrem unübersichtlich. 

variablen_in_snake_case
Der Variablenname beschreibt, was die Variable darstellt. Keine Abkürzungen. 

Funktionsnamen beschreiben, was die Funktion macht. Wenn man einen Wert holt, heißt die Funktion getWert()

Kommentare sind /* Kommentar */
Kommentare zu einzelnen Zeilen fangen eine Leertaste hinter der Zeile an. Kommentare zu Funktionen bzw. zu mehreren Zeilen sind direkt drüber

Wir sollten bei einer Sprache bleiben, basierend auf meiner und deiner Datei wirds Deutsch. Keine Umlaute oder ß, ich glaub die mag ANSI-C nicht (sicher ist sicher)
